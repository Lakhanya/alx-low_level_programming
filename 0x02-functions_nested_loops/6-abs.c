#include "main.h"

/**
 * _abs.c - Computes absolute value of an integer
 * @n: The integer to be computed
 *
 * Return: The absolute valueof an integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
